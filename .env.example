# Google Places API key, used for [describe usage, e.g. place lookups/autocomplete].
# Get a key from https://console.cloud.google.com/apis/credentials
# Copy this file to .env.local and fill in your real key there (never commit .env.local).
GOOGLE_PLACES_API_KEY=YOUR_KEY_HERE

# Supabase project URL and public anon key. Safe to expose to the browser
# (NEXT_PUBLIC_ prefix) — these identify your project and are meant to be
# used from client-side code, protected by Supabase's row-level security.
# Find both in the Supabase dashboard: Project Settings > API.
NEXT_PUBLIC_SUPABASE_URL=YOUR_SUPABASE_URL_HERE
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY_HERE

# Supabase service role key. Bypasses row-level security entirely — this is
# a full admin key. Server-side use ONLY (API routes / server actions),
# never prefix with NEXT_PUBLIC_, never send to the browser.
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY_HERE

# Anthropic API key, used server-side only by the Pricing page's "Assess
# my pricing" tool (app/actions/pricing.ts) to generate an honest,
# tier-based pricing assessment. Get a key from https://console.anthropic.com/
ANTHROPIC_API_KEY=YOUR_KEY_HERE

# Google OAuth client credentials for the Google Business Profile connect
# flow (Phase 1: lib/googleBusinessProfile.ts, app/api/gbp/*). Create an
# OAuth 2.0 Client ID (type "Web application") in the same Google Cloud
# project as GOOGLE_PLACES_API_KEY, under APIs & Services > Credentials:
# https://console.cloud.google.com/apis/credentials
#
# You must also add an Authorized redirect URI there for every
# environment this app runs in, exactly matching this app's own
# /api/gbp/callback route, e.g.:
#   http://localhost:3000/api/gbp/callback   (local dev)
#   https://your-production-domain/api/gbp/callback
# There's no separate redirect-URI env var — it's derived from the
# incoming request's own origin, so it always matches whatever host
# you're actually running on as long as that host's callback URL is
# registered above.
#
# The actual Business Profile scope (business.manage) needs Google's
# approval before real API calls will succeed — until then, the connect
# flow will still run, but real data reads/writes are Phase 2, not this
# one. Leaving these unset shows an honest "not configured yet" state on
# the connect page instead of a broken redirect.
GOOGLE_OAUTH_CLIENT_ID=YOUR_GOOGLE_OAUTH_CLIENT_ID_HERE
GOOGLE_OAUTH_CLIENT_SECRET=YOUR_GOOGLE_OAUTH_CLIENT_SECRET_HERE

# Google PageSpeed Insights API key, used server-side to grade a saved
# business's real website performance/mobile score
# (lib/websiteAnalysis.ts, lib/scoring.ts's website.performance_mobile
# check). Enable "PageSpeed Insights API" in the same Google Cloud
# project as GOOGLE_PLACES_API_KEY (APIs & Services > Library), then
# reuse a key from Credentials, or create a dedicated one. Optional —
# leaving this unset just excludes website.performance_mobile from
# scoring (NOT_FOUND, never scored as a failure) instead of breaking
# anything.
PAGESPEED_API_KEY=YOUR_KEY_HERE

# ScreenshotOne API key (https://screenshotone.com), used server-side to
# capture a real screenshot of a saved business's website for the
# Website page's visual analysis section (lib/websiteAnalysis.ts). Sign
# up for a free account there to get a key — the free tier covers light
# usage. Optional — leaving this unset just shows an honest "couldn't
# capture a preview of this site" state instead of an image; nothing
# else breaks. No separate storage credentials needed: the captured
# image is uploaded to a Supabase Storage bucket in this same project
# (see the "website-screenshots" bucket + policies in
# supabase/schema.sql — run that file's latest additions in the
# Supabase SQL Editor before this feature can show a screenshot).
SCREENSHOT_API_KEY=YOUR_KEY_HERE

# Resend API key (https://resend.com), used server-side only (lib/email.ts)
# to send the monthly "here's what happened" report email. Sign up for a
# free account to get a key — the free tier (3,000 emails/month) covers
# this easily. Optional in the sense that lib/email.ts never throws
# without it — sendEmail() just returns an honest error result — but no
# report emails actually go out until this is set.
RESEND_API_KEY=YOUR_KEY_HERE

# The "from" address the monthly report email is sent from, in Resend's
# "Display Name <address>" format. Leave unset during development to use
# Resend's own sandbox sender (onboarding@resend.dev), which works with
# no domain verification — swap this to a verified sending domain
# (Resend dashboard > Domains) before sending to real users, with no code
# change required.
REPORT_FROM_EMAIL=PostScore <onboarding@resend.dev>

# Bearer secret protecting app/api/cron/monthly-reports (server-side only
# — never prefix with NEXT_PUBLIC_). Every call to that route must send
# `Authorization: Bearer <this value>` exactly, checked with a constant-
# time comparison; missing/unset means the route rejects every request.
# Generate a long random value (e.g. `openssl rand -hex 32`) and set the
# SAME value on whatever scheduler ends up calling this route (a
# not-yet-built piece — see that route's own comments).
CRON_SECRET=YOUR_LONG_RANDOM_SECRET_HERE

# This app's own real base URL, no trailing slash — server-side only,
# used by app/api/cron/monthly-reports to build the monthly report
# email's real unsubscribe link (https://<SITE_URL>/unsubscribe?...).
# Leave unset in local dev to fall back to http://localhost:3000; set to
# your real production domain before this route is ever actually
# scheduled against real owners, or their unsubscribe links will point
# at localhost.
SITE_URL=https://your-real-production-domain.example

# Whether the monthly EMAIL report feature is actually live for real
# users yet — read by lib/monthlyReportsFeatureFlag.ts. The scheduler
# that fires this on a real monthly cadence now exists
# (netlify/functions/monthly-reports-scheduler.mts +
# netlify/functions/monthly-reports-batch.mts), but this should still
# stay unset/false until you've actually deployed it, confirmed the
# schedule shows up in the Netlify dashboard, and are ready for real
# owners to start receiving real emails: while false, the Reports page
# honestly shows "coming soon" for every business regardless of that
# business's own monthly_report_enabled setting or report history. Set
# to the literal string "true" once you're genuinely ready to go live.
MONTHLY_REPORTS_LIVE=false

# Optional override for how many businesses
# netlify/functions/monthly-reports-batch.mts asks the report route to
# process per chunk call (default 3 if unset — see that file's own top
# comment for the timing reasoning behind keeping this small). Only set
# this if real observed timing shows a different number is safer/faster.
MONTHLY_REPORTS_CHUNK_SIZE=3
